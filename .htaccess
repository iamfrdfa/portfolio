# .htaccess im gleichen Ordner wie index.html
Options -MultiViews
RewriteEngine On
RewriteBase /

# Existierende Dateien/Ordner normal ausliefern
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Alle anderen Pfade an index.html übergeben (SPA)
RewriteRule ^ index.html [L]